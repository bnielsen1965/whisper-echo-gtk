#!/usr/bin/make -f
# -*- makefile -*-

%:
	dh $@

override_dh_auto_configure:
	dh_auto_configure -- \
		CMAKE_BUILD_TYPE=Release \
		CMAKE_INSTALL_PREFIX=/usr

override_dh_auto_install:
	dh_auto_install
